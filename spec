[*] Define rules in file, simple CSV to start
[*] Regex rules
[ ] If rule doesn't match, ask at command line 
[ ] Add new rule to file (if not exists)
[ ] Regex for rules
[ ] Rule precedence (most specific rule wins, if not *most recent* rule

[ ] Txs split across multiple accounts
[ ] 2nd pass to clear txs?

